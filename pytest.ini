[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: no network, no subprocess
    integration: requires mock backend subprocess on port 9091
    e2e: requires mock backend and probe engine subprocesses
